env:
  TERM: xterm-256color

window:
  startup_mode:
    Fullscreen

font:
  normal:
    family: Hack
  size: 10

cursor:
  style:
    blinking: Always

  blink_interval: 500
  unfocused_hollow: true

shell:
  program: /usr/local/bin/zsh
  args:
    - --login
